diff --git a/include/linux/sched.h b/include/linux/sched.h
index b02e376..5c5e8a9 100644
--- a/include/linux/sched.h
+++ b/include/linux/sched.h
@@ -831,6 +831,13 @@ struct infinity_ctx {
 	u64		fork_time_ns;
 };
 
+struct infinity_ctx {
+	s64		budget_ns;
+	u64		runtime_debt;
+	u64		last_sleep_ns;
+	u64		fork_time_ns;
+};
+
 struct task_struct {
 #ifdef CONFIG_THREAD_INFO_IN_TASK
 	/*
diff --git a/include/linux/sched.h.rej b/include/linux/sched.h.rej
new file mode 100644
index 0000000..071dc4c
--- /dev/null
+++ b/include/linux/sched.h.rej
@@ -0,0 +1,31 @@
+--- include/linux/sched.h
++++ include/linux/sched.h
+@@ -817,6 +817,20 @@ struct kmap_ctrl {
+ #endif
+ };
+ 
++struct infinity_ctx {
++	s64		budget_ns;
++	u64		runtime_debt;
++	u64		last_sleep_ns;
++	u64		fork_time_ns;
++};
++
++struct infinity_ctx {
++	s64		budget_ns;
++	u64		runtime_debt;
++	u64		last_sleep_ns;
++	u64		fork_time_ns;
++};
++
+ struct task_struct {
+ #ifdef CONFIG_THREAD_INFO_IN_TASK
+ 	/*
+@@ -871,6 +885,7 @@ struct task_struct {
+ 	struct sched_entity		se;
+ 	struct sched_rt_entity		rt;
+ 	struct sched_dl_entity		dl;
++	struct infinity_ctx		infinity;
+ 	struct sched_dl_entity		*dl_server;
+ #ifdef CONFIG_SCHED_CLASS_EXT
+ 	struct sched_ext_entity		scx;
diff --git a/init/init_task.c b/init/init_task.c
index ac87a50..1c309df 100644
--- a/init/init_task.c
+++ b/init/init_task.c
@@ -158,6 +158,11 @@ struct task_struct init_task __aligned(L1_CACHE_BYTES) = {
 				.runtime_debt	= 0,
 				.last_sleep_ns	= 0,
 	},
+	.infinity		= {
+				.budget_ns	= 0,
+				.runtime_debt	= 0,
+				.last_sleep_ns	= 0,
+	},
 	RCU_POINTER_INITIALIZER(real_cred, &init_cred),
 	RCU_POINTER_INITIALIZER(cred, &init_cred),
 	.comm		= INIT_TASK_COMM,
diff --git a/init/init_task.c.rej b/init/init_task.c.rej
new file mode 100644
index 0000000..b999dab
--- /dev/null
+++ b/init/init_task.c.rej
@@ -0,0 +1,19 @@
+--- init/init_task.c
++++ init/init_task.c
+@@ -148,6 +148,16 @@ struct task_struct init_task __aligned(L1_CACHE_BYTES) = {
+ 	.children	= LIST_HEAD_INIT(init_task.children),
+ 	.sibling	= LIST_HEAD_INIT(init_task.sibling),
+ 	.group_leader	= &init_task,
++	.infinity		= {
++				.budget_ns	= 0,
++				.runtime_debt	= 0,
++				.last_sleep_ns	= 0,
++	},
++	.infinity		= {
++				.budget_ns	= 0,
++				.runtime_debt	= 0,
++				.last_sleep_ns	= 0,
++	},
+ 	RCU_POINTER_INITIALIZER(real_cred, &init_cred),
+ 	RCU_POINTER_INITIALIZER(cred, &init_cred),
+ 	.comm		= INIT_TASK_COMM,
diff --git a/kernel/sched/Makefile b/kernel/sched/Makefile
index 6c1d668..bc5edfd 100644
--- a/kernel/sched/Makefile
+++ b/kernel/sched/Makefile
@@ -42,3 +42,4 @@ obj-y += build_policy.o
 obj-y += build_utility.o
 obj-y += infinity_sched.o
 obj-y += infinity_sched.o
+obj-y += infinity_sched.o
diff --git a/kernel/sched/Makefile.rej b/kernel/sched/Makefile.rej
new file mode 100644
index 0000000..ccb4bbe
--- /dev/null
+++ b/kernel/sched/Makefile.rej
@@ -0,0 +1,8 @@
+--- kernel/sched/Makefile
++++ kernel/sched/Makefile
+@@ -40,3 +40,5 @@ obj-y += core.o
+ obj-y += fair.o
+ obj-y += build_policy.o
+ obj-y += build_utility.o
++obj-y += infinity_sched.o
++obj-y += infinity_sched.o
diff --git a/kernel/sched/fair.c b/kernel/sched/fair.c
index 878e80b..3e5a1ce 100644
--- a/kernel/sched/fair.c
+++ b/kernel/sched/fair.c
@@ -59,6 +59,7 @@
 #include "autogroup.h"
 #include "infinity_sched.h"
 #include "infinity_sched.h"
+#include "infinity_sched.h"
 
 /*
  * The initial- and re-scaling of tunables is configurable
@@ -7207,6 +7208,8 @@ static bool dequeue_task_fair(struct rq *rq, struct task_struct *p, int flags)
 	if (flags & DEQUEUE_SLEEP) p->infinity.last_sleep_ns = rq_clock(rq_of(task_cfs_rq(p)));
 	/* Infinity: record sleep start */
 	if (flags & DEQUEUE_SLEEP) p->infinity.last_sleep_ns = rq_clock(rq_of(task_cfs_rq(p)));
+	/* Infinity: record sleep start */
+	if (flags & DEQUEUE_SLEEP) p->infinity.last_sleep_ns = rq_clock(rq_of(task_cfs_rq(p)));
 	if (task_is_throttled(p)) {
 		dequeue_throttled_task(p, flags);
 		return true;
@@ -13504,6 +13507,11 @@ static void task_fork_fair(struct task_struct *p)
 	/* Infinity: init child */
 	infinity_fork_init(&p->infinity, rq_clock(rq));
 
+	struct rq *rq = this_rq();
+
+	/* Infinity: init child */
+	infinity_fork_init(&p->infinity, rq_clock(rq));
+
 	set_task_max_allowed_capacity(p);
 }
 
diff --git a/kernel/sched/fair.c.rej b/kernel/sched/fair.c.rej
new file mode 100644
index 0000000..1295d7d
--- /dev/null
+++ b/kernel/sched/fair.c.rej
@@ -0,0 +1,111 @@
+--- kernel/sched/fair.c
++++ kernel/sched/fair.c
+@@ -57,6 +57,8 @@
+ #include "sched.h"
+ #include "stats.h"
+ #include "autogroup.h"
++#include "infinity_sched.h"
++#include "infinity_sched.h"
+ 
+ /*
+  * The initial- and re-scaling of tunables is configurable
+@@ -1119,8 +1121,16 @@ static bool update_deadline(struct cfs_rq *cfs_rq, struct sched_entity *se)
+ 	 * nice) while the request time r_i is determined by
+ 	 * sysctl_sched_base_slice.
+ 	 */
+-	if (!se->custom_slice)
+-		se->slice = sysctl_sched_base_slice;
++	if (!se->custom_slice) {
++		unsigned long nr = cfs_rq->h_nr_runnable;
++#ifdef CONFIG_SCHED_SMT
++		bool smt_sec = cpumask_weight(cpu_smt_mask(smp_processor_id())) > 1 &&
++		      smp_processor_id() != cpumask_first(cpu_smt_mask(smp_processor_id()));
++#else
++		bool smt_sec = false;
++#endif
++		se->slice = infinity_slice(nr, smt_sec);
++	}
+ 
+ 	/*
+ 	 * EEVDF: vd_i = ve_i + r_i / w_i
+@@ -1298,6 +1308,10 @@ static void update_curr(struct cfs_rq *cfs_rq)
+ 	if (unlikely(delta_exec <= 0))
+ 		return;
+ 
++	/* Infinity: budget consumption */
++	if (entity_is_task(curr)) { struct task_struct *p__ = task_of(curr);
++		infinity_consume(&p__->infinity, delta_exec); }
++
+ 	curr->vruntime += calc_delta_fair(delta_exec, curr);
+ 	resched = update_deadline(cfs_rq, curr);
+ 
+@@ -5162,8 +5176,16 @@ place_entity(struct cfs_rq *cfs_rq, struct sched_entity *se, int flags)
+ 	u64 vslice, vruntime = avg_vruntime(cfs_rq);
+ 	s64 lag = 0;
+ 
+-	if (!se->custom_slice)
+-		se->slice = sysctl_sched_base_slice;
++	if (!se->custom_slice) {
++		unsigned long nr = cfs_rq->h_nr_runnable;
++#ifdef CONFIG_SCHED_SMT
++		bool smt_sec = cpumask_weight(cpu_smt_mask(smp_processor_id())) > 1 &&
++		      smp_processor_id() != cpumask_first(cpu_smt_mask(smp_processor_id()));
++#else
++		bool smt_sec = false;
++#endif
++		se->slice = infinity_slice(nr, smt_sec);
++	}
+ 	vslice = calc_delta_fair(se->slice, se);
+ 
+ 	/*
+@@ -5540,6 +5562,8 @@ pick_next_entity(struct rq *rq, struct cfs_rq *cfs_rq, bool protect)
+ 	struct sched_entity *se;
+ 
+ 	se = pick_eevdf(cfs_rq, protect);
++	if (!se)
++		return NULL;
+ 	if (se->sched_delayed) {
+ 		dequeue_entities(rq, se, DEQUEUE_SLEEP | DEQUEUE_DELAYED);
+ 		/*
+@@ -6937,6 +6961,13 @@ enqueue_task_fair(struct rq *rq, struct task_struct *p, int flags)
+ {
+ 	struct cfs_rq *cfs_rq;
+ 	struct sched_entity *se = &p->se;
++
++	/* Infinity: reset runtime debt on wakeup */
++	if (flags & ENQUEUE_WAKEUP) {
++		u64 now = rq_clock(rq_of(task_cfs_rq(p)));
++		if (p->infinity.last_sleep_ns && now > p->infinity.last_sleep_ns)
++			infinity_refill_budget(&p->infinity, now - p->infinity.last_sleep_ns);
++		p->infinity.runtime_debt = 0; }
+ 	int h_nr_idle = task_has_idle_policy(p);
+ 	int h_nr_runnable = 1;
+ 	int task_new = !(flags & ENQUEUE_WAKEUP);
+@@ -7172,6 +7203,10 @@ static int dequeue_entities(struct rq *rq, struct sched_entity *se, int flags)
+  */
+ static bool dequeue_task_fair(struct rq *rq, struct task_struct *p, int flags)
+ {
++	/* Infinity: record sleep start */
++	if (flags & DEQUEUE_SLEEP) p->infinity.last_sleep_ns = rq_clock(rq_of(task_cfs_rq(p)));
++	/* Infinity: record sleep start */
++	if (flags & DEQUEUE_SLEEP) p->infinity.last_sleep_ns = rq_clock(rq_of(task_cfs_rq(p)));
+ 	if (task_is_throttled(p)) {
+ 		dequeue_throttled_task(p, flags);
+ 		return true;
+@@ -13459,6 +13494,16 @@ static void task_tick_fair(struct rq *rq, struct task_struct *curr, int queued)
+  */
+ static void task_fork_fair(struct task_struct *p)
+ {
++	struct rq *rq = this_rq();
++
++	/* Infinity: init child */
++	infinity_fork_init(&p->infinity, rq_clock(rq));
++
++	struct rq *rq = this_rq();
++
++	/* Infinity: init child */
++	infinity_fork_init(&p->infinity, rq_clock(rq));
++
+ 	set_task_max_allowed_capacity(p);
+ }
+ 
